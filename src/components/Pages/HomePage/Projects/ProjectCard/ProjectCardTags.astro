---
import type { ProjectTag } from "../../../../../data";

interface Props {
  tags: ProjectTag[];
}

const { tags } = Astro.props;
---

<div class="flex flex-row flex-wrap gap-4 items-center text-sm text-subtext-0">
  {tags.map((tag) => <span>#{tag}</span>)}
</div>
