---
import { getLangFromUrl, useTranslation } from "../config";

interface Props {
  title: string;
}

const [language] = getLangFromUrl(Astro.url);
const { t } = useTranslation({ language });

const { title } = Astro.props;
---

<h1 class="text-2xl font-bold">{"> ".concat(t(title))}</h1>
