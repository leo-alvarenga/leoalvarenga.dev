apiVersion: v1
kind: Service
metadata:
  name: {{ include "portfolio.name" . }}
spec:
  selector:
    {{- include "portfolio.labels" . | nindent 4 }}
  type: LoadBalancer
  ports:
  - name: {{ include "portfolio.name" . }}
    port: {{ .Values.service.port }}
    targetPort: 80

